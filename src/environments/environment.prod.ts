/**
 * Production environment settings.
 * Replace `apiUrl` with the URL of your production backend.
 */
export const environment = {
  production: true,
  apiUrl: 'https://your-production-domain.com/api'
};
